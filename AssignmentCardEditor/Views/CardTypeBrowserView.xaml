<UserControl
    x:Class="AssignmentCardEditor.Views.CardTypeBrowserView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:AssignmentCardEditor.ViewModels"
    d:DataContext="{d:DesignInstance viewModels:CardTypeBrowserViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <ListBox
            Margin="5"
            Padding="0,0,0,5"
            BorderBrush="DimGray"
            BorderThickness="2"
            ItemsSource="{Binding CardTypeNameCollection}"
            SelectedItem="{Binding SelectedCardType}" />
    </Grid>
</UserControl>